spring:
  application:
    name: studentsapi

  r2dbc:
    url: r2dbc:h2:mem:///studentsdb?options=DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

  sql:
    init:
      mode: always 
    
server:
  port: 8080

logging:
  level:
    '[com.scotiabank.studentsapi]': DEBUG
    '[org.springframework.r2dbc: DEBUG]': DEBUG